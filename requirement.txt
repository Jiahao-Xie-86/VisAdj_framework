# Core deep-learning stack
# For a specific CUDA build of PyTorch, install torch/torchvision from the
# official PyTorch index first, then run: pip install -r requirement.txt
torch>=2.3,<3.0
torchvision>=0.18,<1.0
pytorch-lightning>=2.2,<3.0
torchmetrics>=1.3,<2.0
tensorboard>=2.14

# Vision foundation model dependencies
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
SAM-2 @ git+https://github.com/facebookresearch/segment-anything-2.git
hydra-core>=1.3,<2.0
omegaconf>=2.3,<3.0
iopath>=0.1.10

# Scientific Python
numpy>=1.24,<2.0
scipy>=1.10,<2.0
scikit-learn>=1.3,<2.0
scikit-image>=0.22,<1.0
networkx>=3.0,<4.0

# Image processing and plotting
opencv-python-headless>=4.8,<5.0
pillow>=10.0
matplotlib>=3.7

# Utilities
tqdm>=4.66
PyYAML>=6.0

# Optional:
# SAM3 support requires the official facebookresearch/sam3 package, which is
# not distributed as a standard pip package. Install it separately if needed:
#   git clone https://github.com/facebookresearch/sam3.git
#   pip install -e sam3
